//allow teachers to upload exam files to the app, combine together with table.js and put into teachers view
import React from 'react'
import Box from '@mui/material/Box';

const Upload = () => {
    return (
      <div>
        <Box textAlign= 'center' paddingTop='150px'>(an option for teachers to upload exam papers/quiz should also be available)</Box>

      </div>
    )
  }
  
  export default Upload